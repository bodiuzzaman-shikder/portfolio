# OS / editor / cloud-sync junk — keep the repo clean
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.log
~$*
.vscode/

# ⛔ PRIVATE — never publish. Personal source material, IDs, CVs, raw docs.
# This folder is a working resource only and must stay OUT of the public repo.
Personal Data files/

